# Copia este archivo como .env y completa los valores reales.
# La cadena de conexión incluye la contraseña: NO la subas a git.
DB_CONNECTION=Data Source=SERVIDOR;Initial Catalog=BASE_DE_DATOS;User ID=USUARIO;Password=CONTRASEÑA
DB_PACIENTE_ID=80420
# Opcional: URL base por defecto (si no se define, usa config.js → http://localhost:4200)
# BASE_URL=http://localhost:4200
